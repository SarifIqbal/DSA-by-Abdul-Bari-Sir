#include <stdio.h>
int main()
{
    char temp;  // temp character is created and it takes one byte of memory
    temp = 'A'; // now characted A is assigned to temp
    printf("Character stored in temp: %c\n", temp);
    printf("ASCII value of character stored in temp: %d\n", temp);
    return 0;
}